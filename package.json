{
  "name": "contextflow-mcp",
  "version": "1.0.2",
  "description": "MCP server for Contextflow — semantic code search and AI chat over vectorized repositories",
  "type": "module",
  "bin": {
    "contextflow-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
